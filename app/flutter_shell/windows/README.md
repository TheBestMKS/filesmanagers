# Windows Runner

Flutter Windows runner with `crypt_core.dll` built from `core/crypt_core` and
installed next to `filesmanagers.exe` for Dart FFI loading.

Build from `app/flutter_shell`:

```powershell
flutter build windows --release
```
