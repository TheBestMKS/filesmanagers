# Защищенное хранилище файлов

Версия приложения: `0.8.0`.

SecureVault - кроссплатформенный защищенный проводник для просмотра, шифрования, выгрузки, загрузки, редактирования и организации файлов. README ведется как карта проекта: сверху зафиксирован полный целевой функционал, ниже указано, что уже реализовано, какие ограничения остаются и что делать в следующих версиях.

## Полный целевой функционал

- Интерфейс как у проводника: расположения слева, файлы/папки по центру, область просмотра справа, изменяемая ширина областей, скрываемая область просмотра.
- Расположения: диски Windows, корень Linux/Android, стандартная область файлов телефона Android, скрытая папка программы, SMB/SSH/FTP/SFTP, облачные хранилища через JSON-плагины.
- Данные программы по умолчанию хранятся рядом с программой в `SecureVaultData`; в настройках можно переключить хранение в папку пользователя.
- Экспорт конфигурации, настроек, языков, плагинов и служебных файлов в ZIP-архив.
- Плагины: облачные хранилища, потоковые сайты, API/парсинг аудио и видео, поиск, плейлисты, подборки, обложки, версии, ссылки обновления, кроссплатформенные компоненты для разных ОС/архитектур, прокси на уровне программы, всех плагинов и отдельного плагина.
- Шаблоны плагинов: Яндекс.Диск WebDAV и Nextcloud WebDAV; целевой executor должен выполнять list/info/stream/upload/free-space и обновления.
- Объединение нескольких облачных хранилищ как одного расширяемого расположения с распределением файлов по свободному месту и оптимизацией размещения.
- Загрузка/выгрузка файлов как в проводнике: encrypted/plain, удаление источника после операции.
- Контекстные меню файлов, папок и пустой области: открыть, создать, создать папку, копировать, вырезать, вставить, удалить, переименовать, свойства, отправить, ZIP-операции, шифровать/расшифровать, использовать папку как галерею/видео/аудио/мультимедиа.
- Создание обычных и зашифрованных файлов, CSV-таблиц, изображений; для CSV выбирается разделитель, для изображения формат, размер, фон и прозрачность.
- Шифрование файлов общим или уникальным ключом, выбор алгоритма, соли/параметры, ключ строкой или файлом; общий ключ настраивается в настройках.
- Пароль входа и отдельный пароль зашифрованных файлов, сохранение паролей в зашифрованном виде, очистка сохраненных паролей при ошибках входа.
- Зашифрованный конфигурационный файл; целевой режим - при пароле входа шифровать конфигурацию этим паролем, иначе встроенным ключом.
- Автоматическое отображение расшифрованных имен файлов и папок при доступном ключе; настройка отключения.
- Недавние и избранные файлы/папки в левой панели с настройкой количества, проверкой доступности и удалением отсутствующих записей.
- Поиск по имени, содержимому или имени+содержимому, регулярные выражения, поиск в подпапках, фильтры по размеру/датам/длительности.
- Просмотр документов, текста, HTML, изображений, аудио и видео обычных и зашифрованных файлов; зашифрованное содержимое должно обрабатываться в памяти и не раскрываться в постоянное хранилище.
- Аудио/видео плейлист внизу области просмотра, повтор, случайное воспроизведение, переход к следующему файлу, запоминание позиции остановки.
- Фоновое воспроизведение: мини-видео и мини-аудиоплеер, перетаскивание по окну, сворачивание/разворачивание/закрытие, настройки отключения фонового поведения.
- Галерея как на телефоне: изображения и видео по годам/месяцам, миниатюры, масштабирование Ctrl+колесико до календарного вида.
- Музыка и Видео как медиатеки: вкладки Все, Плейлисты, Альбомы, Исполнители, Папки; обложки аудио из метаданных.
- Торрент-раздел: добавление `.torrent`, хранение в папке программы, просмотр как папки, потоковое воспроизведение и выборочная загрузка.
- Редакторы отдельными окнами: изображение со слоями, crop/rotate/draw/erase/import layer; аудио с waveform/trim/glue/mix; видео с timeline/trim/concat/resize/rotate/quality/audio track/frame layers.
- Многоязычность: русский и английский по умолчанию, импорт языковых JSON-файлов, экспорт английского образца.
- Защита от скриншотов/записи экрана там, где это позволяет платформа; предупреждение при открытии через внешние приложения.
- Android: запрос прав на файлы/медиа, запоминание отказа, повторный запрос из настроек.
- Сборки Windows, Android и Linux; Linux release собирается на Linux-хосте.

## Реализовано к версии 0.8.0

- Версия повышена до `0.8.0`.
- Для аудио и видео добавлено сохранение позиции остановки: при смене трека, закрытии preview или перестроении плейлиста текущая позиция сохраняется в зашифрованной конфигурации и восстанавливается при повторном открытии файла.
- Плейлист preview теперь передает стабильный ключ файла, поэтому позиция запоминается отдельно для каждого локального, расшифрованного или обычного медиафайла.
- Галерея сгруппирована по годам и месяцам: изображения и видео отображаются секциями `год + месяц`, как в телефонной галерее.
- Добавлен zoom галереи через `Ctrl + колесико мыши`: крупная сетка уменьшается до календарного вида по годам, где месяцы с найденными фото или видео отмечены точкой.
- Для галереи добавлены локализованные названия месяцев и короткие подписи месяцев на русском и английском языках.
- Обновлены встроенные языковые файлы `ru.json` и `en.json`, чтобы новые элементы интерфейса не показывались техническими ключами.
- `flutter analyze` и `flutter test` проходят.
## Реализовано к версии 0.7.0

- Версия повышена до `0.7.0`.
- Данные приложения по умолчанию перенесены в папку рядом с программой: `SecureVaultData`. Для разработки используется папка проекта Flutter, для собранного Windows/Linux приложения - папка рядом с исполняемым файлом. Если запись рядом с программой невозможна, используется безопасный fallback в пользовательскую папку.
- В настройках добавлен переключатель хранения настроек в папке пользователя и экспорт конфигурации/настроек/языков/плагинов в ZIP.
- Настройки расширены: запоминание последней открытой папки, политика перехода в недоступные расположения, масштаб всего интерфейса, масштаб иконок верхней панели, режим поиска по умолчанию, прокси программы и общий прокси плагинов, параметры фонового медиа-поведения.
- При запуске программа восстанавливает последнюю открытую папку, если настройка включена и путь доступен.
- Исправлен переход назад: кнопка вверх больше не использует `Uri.resolve('..')`, добавлена защита от двойного срабатывания, при невозможном переходе используется политика навигации.
- В списке проводника добавлена синтетическая папка `...` для перехода на уровень выше.
- В контекстное меню добавлено создание папки.
- По правому клику/долгому нажатию в пустой области проводника доступно меню: вставить, создать папку, создать обычный/зашифрованный файл, CSV и изображение.
- Для папки добавлены пункты `Использовать как > Папку для галереи/видео/аудио/мультимедиа`, которые добавляют путь в настройки медиа-разделов.
- Поиск расширен: режимы `по названию`, `по содержимому`, `по названию и содержимому`, регулярные выражения и поиск по подпапкам. Для содержимого сейчас читаются текстовые/HTML-файлы и доступные текстовые preview зашифрованных файлов.
- Модель плагинов расширена полями `version`, `pluginType`, `repositoryUrl`, `updateUrl`, `capabilities`, `components`, `platformComponents`, `proxy`, `mediaCatalog`; шаблоны Яндекс.Диск и Nextcloud обновлены под кроссплатформенное описание.
- Добавлена возможность программного экспорта папки плагина в ZIP; экспорт всей конфигурации уже включает установленные плагины.
- Окно просмотра во весь экран получило меню с тремя вертикальными точками.
- Масштаб текста всего интерфейса применяется через `MediaQuery`, масштаб иконок панели пути/поиска применяется в верхней панели проводника.
- Обновлены русские и английские языковые файлы.
- `flutter analyze` и `flutter test` проходят.

## Реализовано ранее к 0.6.0

- Проводник с левой панелью расположений, списком файлов и областью просмотра.
- Изменяемая ширина левой панели и области просмотра; область просмотра можно скрывать.
- Иконки загрузки/выгрузки сверху проводника.
- Локальные расположения Windows/Linux/Android, скрытое хранилище программы, зарезервированные сетевые расположения.
- Установка ZIP-плагинов через настройки и шаблоны WebDAV для Яндекс.Диска/Nextcloud.
- Загрузка/выгрузка plain/encrypted файлов.
- Шифрование app-created `.crypt` через XChaCha20-Poly1305 или AES-256-GCM с Argon2id-derived ключом, солью и метаданными.
- Общий ключ шифрования, ключ-файл, просмотр текущего общего ключа с учетом настроек защиты.
- Пароль входа, отдельный пароль зашифрованных файлов, grace-период, сохранение/очистка сохраненных паролей.
- Зашифрованное хранение настроек на встроенном ключе приложения.
- Избранные, недавние файлы, проверка отсутствующих недавних файлов.
- Контекстные меню файлов/папок с основными операциями, ZIP-распаковка, шифрование/расшифрование файла.
- Создание обычных/зашифрованных файлов, CSV и изображений.
- Просмотр текста, документов, HTML, изображений, аудио и видео в области просмотра; плейлист аудио/видео по папке, repeat/shuffle/loop.
- Переход между изображениями стрелками и свайпом, в области просмотра и во весь экран.
- Разделы Галерея, Музыка, Видео, Документы, Торрент; медиа-разделы сканируют настроенные или доступные пути.
- Обложки аудио через Android `MediaMetadataRetriever` и MP3 ID3/APIC fallback.
- Базовый редактор изображений и базовый аудио/видео-редактор через `ffmpeg`.
- Android `FLAG_SECURE`, запрос файловых разрешений, кнопка повторного запроса в настройках.
- Исправлен заголовок окна Windows и кодировка заголовка.
- Логотип взят из папки `icons`.
- Windows и Android release 0.6.0 собирались.

## Ограничения текущей версии

- Полное шифрование конфигурации паролем входа еще не завершено: сейчас настройки шифруются встроенным ключом приложения. Переключение папки хранения уже работает, но режим `device-key first, app-password fallback` остается следующим этапом.
- Полноценный executor сетевых/облачных/стриминговых плагинов еще не подключен. Сейчас есть формат манифеста, установка ZIP, шаблоны и экспорт.
- Фоновый мини-плеер и PiP-поведение пока представлены настройками и планом, но общий долгоживущий player-сервис еще не вынесен из области preview.
- MediaKit на native-платформах при `Media.memory` сам создает временный файл. Поэтому для строгого требования “зашифрованное аудио/видео без временных файлов вообще” нужен отдельный streaming adapter, который отдает расшифрованный поток декодеру без записи на диск.
- Рекурсивное шифрование папок, создание контейнера из папки и контейнеры TrueCrypt/VeraCrypt пока остаются в меню/плане без полноценного mount-adapter.
- Фильтры поиска по размеру/датам/длительности еще не подключены к поисковому движку; реализованы режимы имени/содержимого/regex/подпапок.
- Галерея уже группируется по годам/месяцам и имеет календарный zoom-режим; следующие улучшения - быстрые переходы по датам, сортировка по EXIF/медиаметаданным и кэш миниатюр.
- Музыка/Видео пока не имеют вкладок Альбомы/Исполнители/Папки на основе медиаметаданных.
- Редакторы аудио/видео пока используют `ffmpeg`-форму без полноценной waveform/timeline GUI и покадровых слоев.
- Прогресс рекурсивного шифрования/расшифрования в отдельном потоке с отменой еще не подключен.
- Torrent adapter с потоковым воспроизведением `.torrent` еще не реализован.
- Linux release не собирается в этой Windows-среде; его нужно собирать на Linux-хосте с Linux desktop toolchain.

## Следующая версия

- Вынести медиа-воспроизведение в общий сервис состояния, чтобы работали мини-плеер, фон и нижний плейлист вне preview; сохранение позиции уже реализовано на уровне preview-плеера.
- Реализовать безопасный in-memory streaming adapter для зашифрованного аудио/видео без временных файлов.
- Довести настройки до структуры подразделов с автосохранением без кнопки `Сохранить настройки`.
- Подключить фильтры поиска по размеру, датам, длительности и индексирование содержимого.
- Добавить вкладки Все/Плейлисты/Альбомы/Исполнители/Папки в Музыку и Видео.
- Реализовать executor JSON-плагинов, обновления плагинов, отдельные proxy-настройки по plugin id и streaming-site плагины.
- Реализовать объединенное облачное расположение с распределением файлов по свободному месту.
- Подключить recursive encryption/decryption jobs с нижним progress panel, сворачиванием и отменой.
- Развить редакторы: image layers/import, audio waveform, video timeline/frame layers.

## Структура проекта

```text
.
|-- app/
|   `-- flutter_shell/
|       |-- android/
|       |-- assets/i18n/
|       |-- lib/
|       |-- test/
|       |-- windows/
|       `-- pubspec.yaml
|-- core/
|   `-- crypt_core/
|-- spec/
|-- dist/
`-- CMakeLists.txt
```

## Проверка и сборка

Нативное ядро:

```powershell
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build --config Release
ctest --test-dir build -C Release --output-on-failure
```

Flutter:

```powershell
cd app/flutter_shell
..\..\tooling\flutter\flutter\bin\flutter.bat analyze
..\..\tooling\flutter\flutter\bin\flutter.bat test
..\..\tooling\flutter\flutter\bin\flutter.bat build windows --release
..\..\tooling\flutter\flutter\bin\flutter.bat build apk --release
```

## Артефакты релиза

После сборки подготовленные файлы находятся в `dist/`:

- `SecureVault-android-0.8.0-release.apk`
- `SecureVault-windows-x64-0.8.0.zip`
- `SHA256SUMS.txt`

Android APK подписан debug-конфигурацией Flutter и подходит для тестовой установки, но не для публикации в Google Play.
